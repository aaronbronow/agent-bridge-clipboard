description = "Accept the latest bridge clipboard value and apply it to the host clipboard"
prompt = """
<task>Accept the latest bridge clipboard value. Inform the user of success:
!{node ./skills/copy/accept.js || bash ./skills/copy/copy.sh --accept || ./skills/copy/copy.sh --accept}
</task>
"""
